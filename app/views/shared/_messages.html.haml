- flash.each do |name, msg|
  - if msg.present?
    %div{:class => "alert alert-dismissable alert-#{name == :notice ? "info" : "danger"}"}
      %button.close{'data-dismiss' => 'alert', type: 'button'} ×
      = content_tag :div, msg, :id => "flash_#{name}" if msg.is_a?(String)
    - flash[name] = nil
